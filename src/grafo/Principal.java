package grafo;

/**
 * Clase Main
 * Punto de entrada principal del programa. Se encarga de la interacción con el usuario,
 * la lectura de datos, y la ejecución de las operaciones del grafo.
 * 
 * Método:
 * - main(String[] args): El método principal que inicia la aplicación. Aquí se deben
 *   leer los datos (por ejemplo, de un archivo), crear instancias de GasStation y Graph,
 *   y luego realizar operaciones como calcular el camino más corto entre gasolineras.
 */
public class Principal {
    public static void main(String[] args) {
        // Lógica principal del programa
    }
}
