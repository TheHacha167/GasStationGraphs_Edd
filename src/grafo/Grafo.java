package grafo;

import java.util.*;

/**
 * Clase Graph
 * Implementa la estructura de un grafo no dirigido y ponderado. 
 * Utiliza estructuras de datos como ArrayList, LinkedList, Hashtable, etc., 
 * para gestionar nodos y aristas.
 * 
 * Métodos:
 * - Graph(): Constructor para inicializar un grafo vacío.
 * - boolean insertarNodo(NodoGrafo nodo): Inserta un nodo en el grafo.
 * - boolean borrarNodo(String clave): Elimina un nodo del grafo.
 * - boolean existeNodo(String clave): Verifica si un nodo existe en el grafo.
 * - int numeroNodos(): Retorna el número de nodos en el grafo.
 * - boolean insertarArista(String clave1, String clave2, Double peso): Inserta una arista en el grafo.
 * - boolean borrarArista(String clave1, String clave2): Elimina una arista del grafo.
 * - boolean existeArista(String clave1, String clave2): Verifica si una arista existe en el grafo.
 * - int numeroAristas(): Retorna el número de aristas en el grafo.
 * - String toString(): Representación en String del grafo.
 * - List<Double> dijkstra(): Implementación del algoritmo de Dijkstra para encontrar el camino más corto.
 */
public class Grafo {
    // Definiciones de atributos y métodos
}
